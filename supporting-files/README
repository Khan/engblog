This directory holds files that you want to link to or include in your
blog post, that are not images.  (You could just put such files in
images/ instead, but that's a pretty misleading name in that case!)
